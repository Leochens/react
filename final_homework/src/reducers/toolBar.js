import * as ActionTypes from '../contants/ActionTypes';


const toolBar = (state = {
  play: false,
  rename: false,
  slice: false,
  share: false,
  delete:false
}, action) => {
  switch (action.type) {
    
    default: return state;
  }
};
export default toolBar;